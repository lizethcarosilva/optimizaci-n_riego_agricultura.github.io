---
import Page from "../layouts/Page.astro";
const pageTitle = "Objetivos";
---

<Page title={pageTitle}>
  <section class="max-w-5xl mx-auto">
    <h1 class="text-4xl font-bold text-emerald-900 mb-10 text-center">
      🎯 Objetivos del Proyecto
    </h1>

    <!-- Objetivo general -->
    <div class="bg-emerald-50 border border-emerald-100 rounded-2xl p-6 mb-8 shadow-sm">
      <h2 class="text-2xl font-semibold text-emerald-800 mb-3">Objetivo General</h2>
      <p class="text-neutral-800 leading-relaxed">
        Desarrollar un sistema de riego inteligente que combine sensores ambientales y
        algoritmos predictivos para optimizar el uso del agua en cultivos, reduciendo el consumo
        sin afectar la productividad agrícola.
      </p>
    </div>

    <!-- Objetivos específicos -->
    <div class="bg-white border border-emerald-100 rounded-2xl p-6 shadow-sm mb-10">
      <h2 class="text-2xl font-semibold text-emerald-800 mb-5">Objetivos Específicos</h2>

      <ul class="space-y-3 text-neutral-800">
        <li class="flex items-start gap-2">
          <span class="text-emerald-600 text-xl">📡</span>
          Diseñar un prototipo con sensores de humedad, temperatura y luminosidad
          controlados por un microcontrolador.
        </li>
        <li class="flex items-start gap-2">
          <span class="text-emerald-600 text-xl">🤖</span>
          Implementar un modelo predictivo (como LSTM o heurístico) para estimar la
          necesidad hídrica diaria.
        </li>
        <li class="flex items-start gap-2">
          <span class="text-emerald-600 text-xl">💧</span>
          Comparar el ahorro de agua y la productividad frente a métodos de riego tradicionales.
        </li>
        <li class="flex items-start gap-2">
          <span class="text-emerald-600 text-xl">🌿</span>
          Evaluar la viabilidad económica y ambiental en el contexto agrícola del Tolima.
        </li>
      </ul>
    </div>

    <!-- Actividad interactiva -->
    <div class="bg-emerald-50 border border-emerald-100 rounded-2xl p-6 mb-10 shadow-sm">
      <h2 class="text-2xl font-semibold text-emerald-800 mb-4 text-center">🧩 ¡Es momento de participar!</h2>
      <p class="text-neutral-700 mb-4">
        Marca los <strong>objetivos específicos válidos</strong> y luego presiona <em>Comprobar</em>.
      </p>

      <div class="space-y-2 mb-4">
        <label class="flex items-center gap-2 p-2 rounded hover:bg-emerald-100 transition">
          <input type="checkbox" class="chk accent-emerald-600" data-correct="1" />
          Diseñar un prototipo con sensores y control.
        </label>
        <label class="flex items-center gap-2 p-2 rounded hover:bg-emerald-100 transition">
          <input type="checkbox" class="chk accent-emerald-600" data-correct="1" />
          Implementar un modelo predictivo.
        </label>
        <label class="flex items-center gap-2 p-2 rounded hover:bg-red-50 transition">
          <input type="checkbox" class="chk accent-red-600" data-correct="0" />
          Vender 1.000 dispositivos el primer mes.
        </label>
        <label class="flex items-center gap-2 p-2 rounded hover:bg-emerald-100 transition">
          <input type="checkbox" class="chk accent-emerald-600" data-correct="1" />
          Evaluar ahorro y productividad.
        </label>
      </div>

      <button
        id="chkBtn"
        class="bg-emerald-600 text-white px-4 py-2 rounded-lg hover:bg-emerald-700 transition font-semibold"
      >
        Comprobar
      </button>

      <div id="chkOut" class="mt-3 font-medium text-neutral-800"></div>

      <script>
        document.getElementById('chkBtn').onclick = () => {
          const boxes = Array.from(document.querySelectorAll('.chk'));
          const correct = boxes.filter(b => b.dataset.correct === '1');
          const ok = boxes.every(b => (b.dataset.correct === '1') === b.checked);
          boxes.forEach(b => {
            b.parentElement.classList.remove('bg-red-50', 'bg-emerald-100');
            if (b.dataset.correct === '1' && b.checked) b.parentElement.classList.add('bg-emerald-100');
            else if (b.checked && b.dataset.correct === '0') b.parentElement.classList.add('bg-red-50');
          });
          const out = document.getElementById('chkOut');
          if (ok) {
            out.textContent = '✅ ¡Muy bien! Todos los objetivos marcados son correctos.';
          } else {
            out.textContent =
              '🔎 Revisa nuevamente. Los objetivos válidos son: ' +
              correct.map(b => b.parentElement.textContent.trim()).join(' · ');
          }
        };
      </script>
    </div>


  </section>
</Page>
