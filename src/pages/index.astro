---
import Page from "../layouts/Page.astro";
const site = new URL(Astro.site);
const basePath = site.pathname.endsWith('/') ? site.pathname : site.pathname + '/';
const to = (path = '/') => {
	const normalized = path.startsWith('/') ? path.slice(1) : path;
	return basePath + normalized;
};
---
<Page title="Optimización de riego agrícola con IA e IoT">
    <h1 class="text-3xl font-semibold text-emerald-900 mb-2">OPTIMIZACIÓN DE RIEGO EN AGRICULTURA</h1>
    <p class="mb-4 text-neutral-700">Sistema inteligente que combina sensores y algoritmos para reducir consumo de agua manteniendo la productividad. Universidad del Tolima · Ingeniería de Sistemas.</p>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 mb-4">
        <div class="bg-white border border-emerald-100 rounded-2xl p-4 shadow-sm">
            <strong class="block text-emerald-900 mb-1">Estudiantes</strong>
            <ul class="list-disc ml-6 text-neutral-700">
				<li>Lizeth Andrea Caro Silva</li>
				<li>Carlos Stiven Villada Quiceno</li>
				<li>Juan Camilo Giraldo Perdomo</li>
			</ul>
		</div>
        <div class="bg-white border border-emerald-100 rounded-2xl p-4 shadow-sm">
            <strong class="block text-emerald-900 mb-1">Docente</strong>
            <div class="text-neutral-700">Adriana Paola Albarracín Calderón</div>
		</div>
	</div>
    <div class="flex flex-wrap gap-2">
        <a href={to('justificacion')} class="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-gradient-to-r from-emerald-600 to-teal-600 text-white shadow hover:-translate-y-0.5 transition">
            <span>📘</span>
            <span>Ir a Justificación</span>
        </a>
        <a href={to('resultados-esperados')} class="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-white border border-emerald-200 text-emerald-900 shadow-sm hover:bg-emerald-50 transition">
            <span>🧠</span>
            <span>Simulación (NN)</span>
        </a>
	</div>
</Page>
